import { MatDialogConfig } from "@angular/material/dialog";

export let matDialogConfig: MatDialogConfig = {
    panelClass: 'dark',
    maxWidth: '95%',
    maxHeight: '90vh',
    closeOnNavigation: true,
    disableClose: true,
} 