
export const FEATURE_CONSTRAINT_TYPE: FeatureConstraintTypes = {
    validOptions: 'ValidOptions',
    validRanges: 'ValidRanges'
}

export interface FeatureConstraintTypes {
    validOptions: string;
    validRanges: string;
}